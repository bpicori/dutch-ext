{
  "name": "tab-taal",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "prettier": "^3.8.4",
    "typescript": "^5.4.0"
  }
}
